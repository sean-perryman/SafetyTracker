<div class="table-responsive well">
  <table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Picture</th>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% @trainings.each do |training| %>
      <tr class='main_row <%= set_index_row_class training %>' data-link="<%= training_path(training) %>">
        <td class="pic-table-cell"><%= cl_image_tag("preset_folder/podlnns8cqw88187vaxh.jpg", :width => 75, :height => 100, :crop => :fill, :class => 'employee-picture center-block') %></td>
        <td><p class="center-block"><%= training.name %></p></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Employee', new_training_path, :class => "btn btn-default button-font center-block new-employee-button" %>